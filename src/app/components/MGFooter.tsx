//Bootstrap
import { Container } from 'react-bootstrap';

export default function MGFooter() {
    return (
        <Container fluid className='cs-bg-navbar'>
            <Container>
                footer
            </Container>
        </Container>
    );
}